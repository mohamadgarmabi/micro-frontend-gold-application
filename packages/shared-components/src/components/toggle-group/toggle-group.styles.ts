import { cva } from 'class-variance-authority'
import { singleComponentStyles } from '../../lib/styles'

const togglegroupClassName = cva(singleComponentStyles.ToggleGroup ?? '')

export { togglegroupClassName }
